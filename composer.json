{
    "require": {
        "tightenco/jigsaw": "^1.7",
        "elaborate-code/jigsaw-localization": "^1.1"
    },
    "scripts": {
       "deploy-ghp": "vendor/bin/jigsaw build production; git add build_production; git commit -m 'Build for deploy'; git subtree push --prefix build_production origin gh-pages"
    }
}
